# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Test Suites

All C++ tests use Catch2 and are built with `-DBUILD_TESTS=ON`.

| Directory | Binary | What it tests |
|-----------|--------|---------------|
| `tests/system/` | `test_system` | Full Scheduler→Worker pool pipeline with real Otterbrix + mock connectors. Tests own an `actor_zeta::scheduler::sharing_scheduler` via `make_az_scheduler()` and await Scheduler/Worker futures through the asio bridge helper `await_session()` (see `tests/system/test_scheduler.cpp`, `test_scheduler_concurrent.cpp`, `test_file_ingestion.cpp`). `test_flightsql_schema_contract.cpp` drives the FlightSQL two-phase path (`prepare_schema` → `to_arrow_schema` → `execute_statement` → `ChunkBatchReader`) over the real engine: a local JOIN streams both same-named key columns under the prepared schema, a DECIMAL column is carried under its own precision and scale and a HUGEINT under `decimal128(38, 0)` — Arrow has no 128-bit integer, and at scale 0 the stored unscaled integer is the value — while an unsigned 128-bit column, which has no carrier either way, is a `conversion_failure` at schema time; a `$1` SELECT has no resolved schema at prepare. `test_single_backend_prepare_schema.cpp` is its remote counterpart over a typed PostgreSQL mock connector (the schema probe answers five columns with their PostgreSQL type oids, the data path answers the projected columns): `prepare_schema` of a SELECT over one remote table — a column list, `SELECT *`, WHERE / ORDER BY / LIMIT — is the STRUCT of the discovered columns in statement order with the discovered types, a `$1` SELECT keeps that STRUCT and carries `parameter_count == 1`, and `execute_statement` streams the backend rows through `ChunkBatchReader` under exactly the prepared schema. `test_prepare_plan.cpp` drives the two pre-built-plan entry points the Spark Connect frontend uses, over a typed PostgreSQL mock connector that records its describe probes apart from its data queries, with the plan parsed by the real `GreenplumParser`: `prepare_plan` of a remote SELECT answers the STRUCT `prepare_schema` answers for its SQL after one describe probe and no data query, and once `close_statement` released it an execute of the session is `invalid_parameter`; `execute_plan` returns the rows `execute` returns for the SQL. The same file pins the Spark frontend's `classify_sql` (a remote SELECT is a query and classifying it asks the backend nothing, not even a describe probe; an INSERT is a command and classifying it writes nothing; a derived-table SELECT is a query, DDL, DML, `CREATE EXTERNAL TABLE` and a Kafka `CREATE SOURCE` are commands, and a text the parser refuses — `EXPLAIN SELECT 1` among them — is its error) and the plan shapes the frontend answers without translating them: a SqlCommand's text from a 3.5 and a 4.x client, a query's SQL relation with its arguments, bare SQL only without arguments, the empty LocalRelation, an unsupported command refused by its name. `test_spark_plan_execution.cpp` (tag `[spark-plan]`) builds the `spark::connect::Plan` a PySpark client sends, translates it with `relation_to_plan` and runs it through `Scheduler::execute_plan` over the real engine, checking the rows rather than the plan's shape: Read and Project with an alias; structured filters (a comparison, the literal on the left, isin, between, stacked filters ANDed, a filter over a select of plain columns and over a `spark.sql` leaf, whose constants share the plan's parameter node); groupBy/agg with count, sum and avg and an unaliased `max` named by its function; `avg` of an integer column answering DOUBLE with its fraction, per group and overall (the engine's own avg over BIGINT drops it); orderBy + limit with Spark's NULL placement, and a filter over those top rows read from a derived table; a filter after orderBy and a row-wise select after orderBy + limit (top-N); crossJoin; `df.count()` and `count(lit(1))` counting every row, globally and per group; an operation on a finished select, distinct or aggregate reading a derived table (`select().count()`, `select().distinct().count()`, `groupBy().agg().select()`, a filter over the aggregate as a HAVING, `select().select()`); a limit on a limit keeping the smaller window, a `spark.sql` leaf's own LIMIT included. In-image `FinalizeS3` listener cleans up Arrow's S3 client at exit (`main.cpp`). `test_constant_aggregates.cpp` (tag `[constant-aggregate]`) runs SQL over the real engine through `Scheduler::execute` and through prepare + execute: `count(1)` and `count('x')` count every row — globally, with WHERE, per group, in HAVING, inside `count(1) + 1`, over 2500 rows (three chunks; the engine's fold answered 3) and over no rows (which crashed the process); `sum(1)`, `avg(2)`, `min(2)`, `count(NULL)` and `count(DISTINCT 1)` are refused naming the function, and an aggregate over a column is untouched. Further files, all over the same mock layer: `test_catalog_manager.cpp` (literal escaping per dialect, GetTables filters, drop-kind classification, discovery error paths — among them a discovered column without a name, refused as `schema_error` — the engine-side undo when a table cannot be mirrored, database-ownership verdicts), `test_backend_discovery.cpp` (pg_enum failure refuses registration, `system.columns` escaping, re-discovery picks up current types), `test_backend_managers.cpp` (among its cases: a MySQL batch whose first slot fails at once while the second slot's converter fails 500 ms later on the io thread — every block the stack frees is zeroed and quarantined by a test `memory_resource`, and no freed block may be written, i.e. the conversion slots outlive the waiter's drain; over a ClickHouse mock connection whose discovery answers `chdb.events` with `Array(Nullable(Int32))`, `Array(Nullable(String))` and a named Tuple, through the real Scheduler stack and engine: a 2500-row slice in two blocks reaches the cursor as chunks within the bound with every LIST element and STRUCT field intact, an engine JOIN on the scalar id against a local table keeps the nested values of the rows it selects, `AVG(score) AS score` — ClickHouse's Float64 under an Int32 column's name — is prepared and executed as the same DOUBLE column and streams through `ChunkBatchReader` under its FlightInfo schema, and the named-type columns are prepared and executed as the same types, Tuple field names included), `test_otterbrix_manager.cpp` (affected-count carrier, `dml_without_returning` / `row_producing_statement` over parsed plans, a column-less result of an aggregate-root plan answered as `schema_error` while an inlined slice passes, `register_external_database` — a fresh database is created, an existing one is reused as the previous run's mirror, any other verdict keeps its code — `drop_external_table`, create_table verdicts), `test_prepared_reexecute.cpp` (a prepared statement is single-use on the Worker; `close_statement` drops an unexecuted entry so the next execute answers re-prepare, and closing a consumed, closed or unknown session is success), `test_dml_result_shape.cpp` (column-less DML payloads; `EXPLAIN DELETE` refused at the Scheduler, rows untouched; constraint cases over the simple and the prepared route: a column and a table CHECK declared in `CREATE TABLE` are enforced on INSERT and UPDATE, the refusal naming the constraint (empty for a column CHECK), a PRIMARY KEY refuses a duplicate key, and a FOREIGN KEY refuses both a child row naming a parent that is not there and a DELETE of a parent a child still references — on the simple route, on prepare + execute, and on a `$n` statement, whose catalog lookups are takeable only once Bind has filled every parameter and so come off the plan `finalize()` answers; a `REFERENCES` target that does not exist is refused at `CREATE TABLE`, and a CHECK added by `ALTER TABLE ... ADD CONSTRAINT` is enforced until `DROP CONSTRAINT` takes it off again; the prepared matrix — `$n` in WHERE, `LIMIT $1`, `IN ($1, $2)`, INSERT / UPDATE / DELETE, a NULL parameter bound into an INSERT and read back through `IS NULL` (the same NULL compared in `WHERE id = $1` carries `[engine-defect-nullparam]`, see "Engine-defect tags"), too few / too many / no parameters with the binder's exact refusals, an execute without Bind refused by the Worker before the engine as `invalid_parameter` `prepared statement executed without binding its N parameter(s)`, `parameter_count` at prepare), `test_external_registration.cpp` (the pg_class oid stamped on create; `insert_data` fills its own database only — a missing database is refused, a second call on an existing table is `table_already_exists` and the table keeps the first load's rows, a table removed by `DROP TABLE` loads again with only the new rows — and `get_schema` answers the same STRUCT for an empty and a populated table), `test_kafka_runtime.cpp` (among its cases: DROP SOURCE with a dependent VIEW neither cascades nor is refused — the view fails until a source of that name exists again), `test_row_producing_result_shape.cpp` (its counterpart, tag `[row-shape]`: a SELECT whose result the engine returns without columns — `SELECT big FROM <hugeint table>` — is a `schema_error` naming the statement on the simple, prepared and `COPY ... TO` routes, never a column-less payload the frontends would report as a DML; an empty SELECT keeps its columns, a no-RETURNING DML keeps its column-less count), `test_clickhouse_dml_count.cpp` (ClickhouseManager over a connector reporting written_rows), `test_database_ownership_guard.cpp`, `test_restart_reconciliation.cpp` (a PostgreSQL mock connection registered, the engine restarted on the SAME data dir and the connection registered again: the mirror database is reused and both tables keep their OIDs, a table whose backend schema changed between the runs is recreated with the new columns under a new OID, a table the backend dropped is dropped from the engine and from the uid's `__otterstax_tables` manifest; the mock connector answers the `information_schema` listing and the LIMIT-0 probes from a table catalog the test switches between runs), `test_connection_retry.cpp`, `test_s3_errors.cpp`, `test_composite_insert.cpp` (composite `ROW(..., NULL)` through INSERT/UPDATE over the scheduler stack — `[engine-defect-e1]`, see "Engine-defect tags"), `test_default_insert.cpp` (a column DEFAULT stored for a column the INSERT omits, every statement run twice — straight against the engine via `make_otterbrix_manager()->execute_sql` and through the scheduler stack — so the two answers can be compared; the `[default]` cases (BIGINT/STRING/DOUBLE DEFAULTs, literal type equal to the column type) are visible and green, the `INT DEFAULT 7` / `DEFAULT ROW(...)` cases carry `[engine-defect-default]`, see "Engine-defect tags"), `test_component_manager_lifecycle.cpp` (the real `ComponentManager` constructed and destroyed unstarted on a fresh data dir, three times in one process — the shape an aborted startup leaves behind; asserts construction initialises Arrow S3, which is why the process must `FinalizeS3`), `test_frontend_shutdown.cpp` (`mysql_server` / `postgres_server` start→stop cycles with no client: the accept chain must not re-arm on the closed acceptor or pool a connection `stop()` would never finish), `test_frontend_accept_retry.cpp` (a `frontend_server` over a `mysql_connection` whose constructor throws on chosen attempts — for the first client, for a later one, three in a row: the client the failures were for gets the MySQL handshake after that many `accept_retry_delay` pauses (the production 100 ms here) and the chain keeps accepting; `stop()` while the retry timer holds a client that was never served, with and without a served client besides, returns under a 10 s watchdog — a miss is `std::_Exit(1)` — and closes every client, twenty cycles each; 1005 failures in a row — more than `MAX_CONNECTIONS` — for one client with a 1 ms `accept_retry_delay`: it gets the handshake, not the too-many-connections refusal, because each failed construction hands its pool slot back), `test_frontend_connection_lifetime.cpp` (both servers on port 0 with `read_timeout` set per test and raw client sockets that complete the MySQL handshake / PG startup: `stop()` with clients attached, the idle read timeout closing the connection and freeing the slot, a client dropping the socket while a read is pending, a PG `StartupMessage` larger than the read buffer, and a PG client that abandons an extended-query pipeline while its connection holds a statement the Worker still keeps (`Parse` + `Describe` of `SELECT 1` + `Sync` read up to `ParseComplete`; a named statement with a portal; two `Parse`s not answered yet) right before the server stops — the client closed first, still attached, and in the destruction order of a failed REQUIRE's unwind (client, server, then a scheduler stack of its own per cycle), so `finish_impl` sends `close_statement` while `stop()` joins the pool — the connection object must outlive every pending asio completion; one freed early shows up as a SIGSEGV/SIGABRT under `MallocScribble=1`), `test_frontend_malformed_packet.cpp` (a client packet shorter than the fields its command carries; the reader never throws. MySQL `COM_STMT_EXECUTE` / `COM_STMT_CLOSE` cut before the statement id, a `HandshakeResponse41` whose user name has no NUL: `ERR 1835` followed by the close. PG after startup — `Parse` / `Describe` with an unterminated name, `Parse` with truncated parameter types, `Bind` / `Execute` cut inside a fixed field, `Close` without its type byte: `ErrorResponse` **ERROR** `08P01` alone, a valid `Describe` sent behind it discarded, the `Sync` answering the one `ReadyForQuery` (`I`: no transaction block is open), and a `SELECT 1` served on the same connection afterwards; a `Query` without its terminator or with an empty body: `ErrorResponse 08P01` + `ReadyForQuery` at once, a lone NUL being the empty query. After `BEGIN` the status is `T` on a simple `Query` and on a `Sync`. It turns `E` once an error failed the block, from a pipeline's `Sync` and from a simple `Query` alike. It is `I` again after `ROLLBACK`, or after a `COMMIT` of the failed block, which is answered `ROLLBACK`. A byte after the last field of `Query` / `Parse` / `Bind` / `Execute` / `Describe` / `Close`, or any body on `Sync` / `Flush`, is `ERROR 08P01` `invalid message format` under the same rule. `Sync` ends the pipeline first, so its error gets `ReadyForQuery` at once, and `Terminate` with a body still closes. A type the protocol does not define is `FATAL 08P01` `invalid frontend message type <n>` followed by the close: `z`, a `PasswordMessage` after startup, a header whose announced body never comes, one inside a failed pipeline. `CopyData` / `CopyDone` / `CopyFail` outside COPY draw no answer. Well-formed messages of every type draw no `ErrorResponse`, so each handler reads its body to the last byte; this includes `Bind` with a binary value of each bindable type, text values and a NULL. A `StartupMessage` with an unterminated key or no room for the protocol version stays `FATAL 08P01` followed by the close. A result column without a name (`SELECT 1`, no alias in the prepared schema) is named as an executed result names it — the name the simple `Query` / `COM_QUERY` shows: PG `Describe(statement)` answers `ParseComplete`, `ParameterDescription`, `RowDescription`, `ReadyForQuery`; `Describe(portal)` then `Execute` answers `RowDescription`, the row and `CommandComplete`; MySQL `COM_STMT_PREPARE` answers `STMT_PREPARE_OK` with one column definition and EOF. The executed column of `SELECT 1 UNION ALL SELECT 2` has no alias either and is named the same way in every resultset built from an executed chunk: MySQL `COM_QUERY` and `COM_STMT_PREPARE` + `COM_STMT_EXECUTE`, PG simple `Query` and `Parse` / `Bind` / `Execute` without `Describe`; a PG `SELECT` over a VIEW (an unnamed executed column on rc-2) is served. Before, `alias()` of that column dereferenced null on the pool thread and killed the process. In every case the server keeps serving). The raw-socket clients both files use (`raw_client`, the MySQL handshake, the PG startup and message framing) live in `raw_wire_client.hpp`. |
| `tests/unit/parser/` | `test_parser` | Subquery extractor, qualifier rewriter, SQL generator, and the s3/file grammar extensions (parse stage, registry routing, transform → `external_node_t`, trailing-`;` tolerance). The `characterization:` cases in `test_query_gen.cpp` pin the whole generated statement or the whole refusal (stage, code, message) per dialect: UPDATE SET constants and NULL, bound `$n`, arithmetic with parentheses, every update-expression kind, operators and functions written as SQL, UPDATE ... FROM / DELETE ... USING refused, DROP / CREATE INDEX, constant WHERE, and DDL on an alias routed to one external slot. `test_parser_integration.cpp` pins SAVEPOINT / ROLLBACK TO / RELEASE as `unimplemented_yet` "Unsupported node type" |
| `tests/unit/parser/grammar_extension/kafka/` | `test_kafka_grammar` | Kafka DDL grammar extension (`kafka_node_t`, `kafka_write_target`) |
| `tests/unit/schema/` | `test_schema` | `schema_utils` namespace |
| `tests/unit/translators/` | `test_unit_translators` | Input translators (mysql/pg/ch/csv/ndjson/parquet → `data_chunk_t`; `test_mysql_types.cpp` pins the ONE MySQL column-type table over result sets whose column definitions are manufactured through `boost::mysql::detail::access`: for every `boost::mysql::column_type` and both signednesses, what `mysql_to_struct` (the schema a discovery mirrors) answers is what `mysql_to_chunk` (the executed chunk) answers — same type, same name, never NA — and a type with no arm (`column_type::unknown`) is the same `conversion_failure` naming the column on both paths; plus the table per type: BIGINT / UBIGINT, YEAR as an integer, BIT as UBIGINT, the date/time family and JSON/ENUM/SET/BINARY/VARBINARY/GEOMETRY as text, and a column-less OK packet as the empty STRUCT; `test_ch_to_chunk.cpp` over one block and over split blocks: a named-type override the wire column represents reads the wire value as the column's type — `Bool` over UInt8, `LowCardinality` / `SimpleAggregateFunction(f, T)` as `T`, a Tuple field declared `Bool`, `Array(Tuple(id Int32, ok Bool))` — each read back non-NULL with the vector's type; a wire column that is not a representation of its override is the wire's type holding the wire's values, and `ch_to_struct` registers the same — `AVG(x) AS x` Float64 under Int32 (also Nullable), `toString(id) AS id`, `count() AS x` UInt64, `x + 1 AS x` / `SUM(qty) AS qty` Int64 inside and past the Int32 range, a negative Int32 under UInt32, Float64 under Int64 or Float32, Float32 under Float64, numbers under `String`, UInt16 under `Bool`, a scalar under a Tuple, a Tuple of fewer or other-typed fields; `Array(T)` is a LIST of each row's own length (0, 1, 3 elements, NULL elements, `Array(Bool)`); DateTime64 / Decimal / Enum / IPv4 / IPv6 / Int128 / UInt128 / Time / `LowCardinality(String)` are their text with and without the `system.columns` override and `ch_to_struct` registers the chunk's types; Map and Nothing are `conversion_failure` naming the column), `split_to_capacity` (`test_chunk_windows.cpp`: 2500 rows from csv, ndjson, parquet, an arrow batch, PostgreSQL tuples and two ClickHouse blocks become chunks of 1024 + 1024 + 452 rows keeping values and NULLs on both sides of each boundary; 2500 ClickHouse rows with LIST (`Array(Nullable(Int32))`, `Array(Nullable(String))`: empty lists, NULL elements) and STRUCT columns keep every row's elements and fields in the windows and in copies of them made the way `operator_raw_data_t` adopts them; a chunk within the bound, a zero-row chunk with columns and a DML count carrier over 1024 rows are handed on whole) and output writers (`chunk_to_{csv,ndjson,parquet,arrow}`). `PGresult` objects are manufactured without a connection: rows through `PQsetResultAttrs`/`PQsetvalue`, the command tag through `libpq-int.h` (`pg_result_fixture.cpp`; the `libpq_internal/getaddrinfo.h` shim and the `${PostgreSQL_INCLUDE_DIRS}/postgresql/internal` include dir exist for that header) |
| `tests/unit/utility/` | `test_utils` | `session` helpers; `wait_barrier` (`QueryHandleWaiter`'s one-per-future/in-order postcondition, which the three integration managers index positionally, plus `make_failed_future`, and `spawn_marshaled` over a strand / `any_io_executor` as well as an io_context; `run_with_owned_handler` keeps a temporary handler alive until the query ran, and `function_ref_t` binds only a named non-const callable); the real `mysql::Connector` against a fake MySQL server on 127.0.0.1:0 (`test_mysql_connector.cpp` — the fake speaks HandshakeV10 with boost.mysql's mandatory capability flags, reads HandshakeResponse41, answers OK, COM_PING and COM_QUIT: a connect/close loop over a multi-threaded pool always yields a value — the `cancel_after` timed_cancel_op race that broke the promise and terminated the server at startup; `close()` and the destructor after the server reset the socket with SO_LINGER 0 do not throw); the real `ch::Connector` against a fake ClickHouse server on 127.0.0.1:0 (`test_ch_connector.cpp` — the fake speaks the native handshake, ClientHello/ServerHello at revision 54459 plus the addendum string, and Ping/Pong: a server that accepts and never answers makes `connect()` return `io_error` in bounded time — one driver attempt per connect call, ≈ 20 s with the case's one-rung reconnect ladder — a server that completed the handshake and then swallows pings makes `isConnected()` false in bounded time, ≈ 10 s; and a query — the `executeQuery` sequence, `isConnected()` then `spawn_marshaled(runQuery)` on a pool io_context — against a server that answers every handshake but no ping after the first two is `io_error` in ≈ 10 s over a single connection with no driver-level Ping in front of it (with `ping_before_query` the driver's `RetryGuard` reconnected every 11 s forever — 6 connections at the 60 s deadline); each blocking call runs under a 60 s deadline and a miss is `FAIL_CHECK` + `std::_Exit(1)`, so a connector left blocked in `recv()` fails the run instead of hanging it); `asio_future_bridge` (`test_asio_future_bridge.cpp`) — waits for the producer, gives up at the deadline, wakes early on either failure channel; `parse_port` (`test_parse_port.cpp`); the connector error classifiers (`test_connector_errors.cpp`); `ch::make_statement` — written_rows accumulated from Progress, blocks collected (`test_ch_statement.cpp`); the real `ConnectorManager::executeQuery/addConnection` with mock connectors over a real CatalogManager (`test_connector_manager.cpp`: unknown uuid, closed connector, a failed reconnect keeps the registration, an exception on the io thread → io_error, a pool that is not running → io_error, a malformed port → invalid_parameter, a failed schema registration is not kept). The binary links `lib_otterstax` for that |
| `tests/unit/config/` | `test_unit_config` | `config::parse_connections` (the `connections:` node → descriptor structs; null/missing node, missing sections, pg `schema` default, optional s3 fields, order; an incomplete entry, a malformed port or a non-scalar field → `invalid_parameter`) and `config::ConfigReader` (full `config.yaml`: ports + embedded connections, the `spark_connect` host/port and its `0.0.0.0:15002` default when the node is absent, defaults when file missing, no-`connections:` section, malformed YAML → `conversion_failure`, a non-integer service port — `service.spark_connect.port` included — or an invalid connection → `invalid_parameter`). Nothing is asserted to throw: every failure is a `core::result_wrapper_t` error. Links `config::config`; `main.cpp` calls `initialize_all_loggers` so the `Config` logger exists |
| `tests/unit/spark_connect/` | `test_unit_spark_connect` | The Spark Connect frontend's parts that need no server: `expression_to_plan` (literals bound as parameters, column keys, aggregates named by their function until an Alias renames them, `count(*)` without arguments, `count` of a literal counted like `count(*)`, an aggregate over a constant argument refused, `avg` over its argument cast to DOUBLE (the column as the cast's key), arithmetic, `==` and isNull as compare(key, parameter), sort orders with Spark's NULL placement), `relation_to_plan` (one to four name parts read as the SQL path reads them, the external nodes and the PostgreSQL schema of a remote read, structured and string filters, consecutive filters ANDed, isin / between / the literal on the left, a filter that is not a predicate refused, the group shape of select and groupBy/agg and a computed grouping key, using-columns joins in key form, crossJoin's match-everything condition, `spark.sql` leaves binding into the shared parameter node and refused when they lower a sub-query or are not a single SELECT, `dropDuplicates` over a subset refused, the operation chains the fixed clause order stacks, an operation it cannot stack reading a derived table, a limit or an offset on a limit merged into one window, a derived table read through a renamed column refused as `unimplemented_yet`, a remote read under a derived table staying the external slot, `unionByName` refused, `spark.range` as a column named `id`, window detection), `type_converter` (the Spark `DataType` per logical type, `col<i>` for an unnamed field), `result_encoder` (an empty chunk is a valid IPC stream, unsigned columns re-tagged signed, a column the Arrow batch cannot carry is a `conversion_failure`), `show_string` (`test_show_string.cpp`, Spark's `showString` text: framing and right alignment when truncating, left alignment with truncate 0, a long cell cut with `...` and a header never cut, `NULL`, the footer without a trailing newline, an empty result and one without columns, vertical records, the schema's names and `col<i>`, escaped control characters, full-width characters, each type's cell spelling, an unrenderable type as `conversion_failure`, the one-row `show_string` answer, the input fetched under a limit of one row past the shown ones) and the catalog rows (`test_catalog_relations.cpp` over `catalog_rows.hpp` with synthetic pg_catalog rows: the databases, the tables classified and the internal ones hidden, OIDs of any integer type, unreadable rows as `schema_error`, a table whose database was dropped between the two reads, Spark name patterns, lookup by the identifier the SQL path reads, the `getTable` refusals, the column probe's quoting, and every row layout PySpark reads by position, in `DEFAULT_VECTOR_CAPACITY`-row chunks). Links `spark_connect_server` and `lib_otterstax` |
| `tests/mysql-front/` | `test_mysql_front` | MySQL protocol reader/writer (`test_reader_writer.cpp` — every read primitive on a buffer shorter than the field reports `packet_fault::underflow`, a 0xFB/0xFF length prefix `invalid_marker`, the position does not move and the fault is sticky; nothing throws), result-set encoding; also the wire-level tests of the other two frontends, which need no running server: PG packets (`test_pg_frontend_packets.cpp` — ParameterDescription, PortalSuspended, CloseComplete, RowDescription format codes, `parse_text_parameter`, `pipeline_state`: an error inside an extended-query pipeline defers ReadyForQuery to the Sync, one outside it does not, and `transaction_manager`: `mark_failed` fails an open block only (`I` stays `I`, `T` becomes `E`), and COMMIT of a failed block answers `ROLLBACK` + `I`, like ROLLBACK), the FlightSQL ticket codec (`test_flight_ticket.cpp`) and `ChunkBatchReader` (`test_chunk_batch_reader.cpp`); `test_wire_type_support.cpp` — `get_field_type` answers nullopt (never throws) for a logical type the MySQL/PG wire has no type for (UNKNOWN, UHUGEINT, DATE, …), while DECIMAL and HUGEINT are carried as `NEWDECIMAL` / `NUMERIC` (1700) with the value rendered from its unscaled integer, so refusal is per FORMAT rather than per type: PostgreSQL binary refuses both (the `numeric_send` layout is the backend's internal business, and a byte off desynchronises the whole DataRow), MySQL carries them in text and binary alike. `is_encodable` / `find_unsupported_column` name the first column a resultset could not carry under the Bind result formats (nested types pass in text, are refused in binary), `unsupported_column_message` names column, type and format |

The binary name is the `project()` name in each directory's `CMakeLists.txt`; the
binaries are built under `build/<type>/tests/<dir>/` (e.g.
`build/Release/tests/unit/utility/test_utils`).

Python integration tests in `tests/` (e.g. `test_flightsql_client_mysql_backend.py`) run against live Docker containers via `docker-run-tests.sh`. `test_flightsql_client_otb_local.py` covers the FlightSQL GetFlightInfo/DoGet contract over otterbrix-internal tables: a local JOIN streams both same-named key columns and the stream schema equals the FlightInfo schema; a `$1` placeholder and an unsigned 128-bit column are refused by GetFlightInfo before a ticket is issued, while a DECIMAL column streams keeping its own scale and a HUGEINT streams as `decimal128(38, 0)`. `test_multichunk_frontends.py` pushes >1024-row results (the engine's chunk cap) through PG simple/extended, MySQL text/binary (COM_STMT_EXECUTE), FlightSQL DoGet and `COPY ... TO` file and s3, checking the content row by row. `test_pg_client_otb_local_dml.py` covers local DML row counts on three wire shapes (simple, unnamed extended, named `prepare=True`) and a 2000-row DML. `test_pg_extended_protocol.py` speaks the PG extended protocol on a raw socket: Execute row limits and PortalSuspended, ParameterDescription, a named statement executed twice and after a failure (the frontend re-prepares; the Worker keeps a statement for one execution), Bind refusing a malformed literal before BindComplete, binary result format codes, a `hugeint` column answered with `ErrorResponse 0A000` naming it (simple Query, Describe, Execute) while the connection keeps serving, a `SELECT big` of that column alone — which the engine answers without columns — refused with an `ErrorResponse` naming the statement and never a `CommandComplete` (simple Query and an Execute without Describe), a named statement parsed but never executed followed by Terminate / a socket drop leaving the server serving (the teardown's `close_statement` is not observable on the wire; the case proves the path completes), and a message truncated inside its fields after startup (an `Execute` cut before its row limit, a `Query` without its terminator) answered with `ErrorResponse` **ERROR** `08P01` — inside a pipeline deferred to the `Sync`'s single `ReadyForQuery`, at once for the simple `Query` — while the same connection keeps serving. The same file pins the `ReadyForQuery` status as the transaction block's. It is `I` after a failed pipeline outside a block. It is `T` after `BEGIN`, on a simple `Query` and on a `Sync`. It turns `E` once an error inside the block failed it, from a pipeline's `Sync` and from a simple `Query` alike. It is `I` again after `ROLLBACK`, or after a `COMMIT` of the failed block, which is answered `ROLLBACK`. `test_mysql_wire_protocol.py` is its MySQL counterpart on the raw socket (hand-framed HandshakeResponse41, `COM_QUERY`, `COM_STMT_PREPARE`, `COM_QUIT`): the `hugeint` column is an `ERR 1235` packet naming it for `COM_QUERY` and `COM_STMT_PREPARE`, a `COM_QUERY` of that column alone is an `ERR` packet naming the statement rather than the OK packet a column-less payload would produce, and a statement prepared then abandoned at `COM_QUIT` / socket drop leaves the server serving. The PG file's `read_failed_pipeline` asserts the protocol's error rule: after an error inside an extended-query pipeline the frontend sends the ErrorResponse alone, discards the pipeline up to its Sync, and the Sync answers exactly one ReadyForQuery with nothing trailing it (a failed simple Query keeps ErrorResponse + ReadyForQuery at once); the "named statement after a failure" case fails its Execute with an INSERT of a text literal into a bigint column, which the engine refuses when the statement runs (a repeated `CREATE TABLE` of an existing table is not an error). `test_flightsql_client_pg_backend.py` also asserts that a `$1` SELECT over the PostgreSQL backend is refused by GetFlightInfo with "unbound parameter" — the remote counterpart of the local refusal. The `*_client_mysql_backend.py` pair asserts remote DML row counts (`test_remote_dml_row_counts`) and that a statement MariaDB refuses carries MariaDB's own message (`test_backend_error_carries_server_message`); the `*_client_ch_backend.py` pair asserts ClickHouse's counts (INSERT from written_rows, mutations 0).

The Spark Connect files — `test_spark_client_{mysql,pg,cross}_backend.py`, `test_spark_client_dataframe_ops.py` (DataFrame operations over `spark.table()` of both mirrors and of a local table, each checked against the same query written as SQL — the chains that read a derived table included: `select().count()`, `distinct().count()`, `limit().first()`, `groupBy().agg().select()`, a filter over an aggregate, `select().select()`, a `spark.sql` LIMIT under `limit()` — and `unionByName()` over reordered columns refused; `avg` over the local table's BIGINT column is checked against the mean of its rows instead, since the SQL path's AVG still drops the fraction), `test_spark_client_sql_and_show.py` (`spark.sql()` running its statement once: derived-table queries over the MySQL and PostgreSQL mirrors, `count()`, DDL and an INSERT that collecting their DataFrames does not run again; `df.show()` with its defaults, with the footer, untruncated, vertical and over a `spark.sql()` query, against text rebuilt by Spark's rules) and `test_spark_client_catalog.py` (`spark.catalog` over both mirrors and a local database made through `spark.sql()`) — are not in the standard suite (`test-client`). They run under every released PySpark client — 3.5.0–3.5.9, 4.0.0–4.0.4, 4.1.0–4.1.3 and 4.2.0, 20 versions — in an image of their own, `Dockerfile.spark-test` (compose service `test-spark-client`): Python 3.12, plus Python 3.11 for 3.5.x, and no JVM; one dependency venv per minor line (4.1 and 4.2 share one) and each client installed on its own with `pip install --no-deps --target /opt/pyspark/<version>` (`tests/scripts/install_spark_clients.sh`: 3.5.x the `pyspark` sdist with its jars removed, 4.x `pyspark-client`). `tests/scripts/run_spark_matrix.sh` runs every `test_spark_client_*.py` under each version — six files × 20 versions — `SPARK_MATRIX_JOBS` versions at a time (default 4), and prints a version × file PASS/FAIL table; `docker-run-tests.sh` runs it as a separate step after the standard suite. The three integration workflows, ASAN and TSAN included, run the full matrix: they pre-build the image with the buildx GHA cache scope `spark-client` and pass its tag in `SPARK_CLIENT_IMAGE`, which makes `docker-run-tests.sh` skip building it. The files use only API present in PySpark 3.5, so each runs unchanged on every client; several clients run against one server at once, so a file that creates objects names them uniquely.

## Mock Layer (`tests/mock/`)

System tests build a full actor graph using real Otterbrix but injected mock connectors:

| Mock file | Replaces |
|-----------|----------|
| `mock/parser.hpp` | `IParser` — returns a pre-built `ParsedQueryData`; `make_mock_parser` / `make_throwing_mock_parser` are passed as `parser_factory_fn` to the Scheduler so each `Worker` builds its own mock instance (actors share no parser) |
| `mock/sql_db_connector.hpp` | `mysql::IConnector` — returns fixed result rows; the schema-discovery overload always succeeds with an empty result set |
| `mock/pg_db_connector.hpp` | `pg::IConnector` |
| `mock/ch_db_connector.hpp` | `ch::IConnector` |
| `mock/otterbrix.hpp` | `IDataManager` (`SimpleMockOtterbrixManager`; the multi-chunk mode returns `(id, name)` rows whose values are checkable per row) |
| `mock/mock_config.hpp` | Knobs shared by the mocks (`resource`, `can_throw`, `return_empty`, `wait_time`, `error_message`) |

Connectors are injected via the `connector_factory` constructor parameter on `ConnectorManager`. Otterbrix is injected via `OtterbrixManager(resource, make_unique<SimpleMockOtterbrixManager>(mock_config{.resource = resource}))`. The CatalogManager holds no `ConnectorManager*`: fixtures spawn the three integration actors and call `catalog->set_backend_managers(mysql, pg, ch)` with their addresses (`actor_zeta::address_t::empty_address()` for a backend the test has none of) before any `addConnection`. `ConnectorManager::addConnection` keeps a connection only when the catalog registered its schema, so a mock MySQL connection must name a database (`mock_connect_params()` in `test_scheduler.cpp`) and the tests assert the returned `result_wrapper_t` carries no error.

## Running a Single Test

```bash
# Run one Catch2 test by name
./build/test_system "[test name]"

# List all available test names
./build/test_system --list-tests

# Run with verbose output
./build/test_system -s
```

Catch2 splits a test-name filter on commas, and shell quoting does not help:
Catch2 splits the spec itself, and a spec whose own `"` quotes hold a comma is
refused outright (`Invalid Filter`). A name that contains a comma therefore
runs only with the comma backslash-escaped, by a comma-free `*` fragment, or by
its tag:

```bash
./build/Release/tests/unit/utility/test_utils 'parse_port: out-of-range values are invalid_parameter\, not truncated'
./build/Release/tests/unit/utility/test_utils 'parse_port: out-of-range values are invalid_parameter*'
```

Give a new case a comma-free name so it stays runnable exactly as it is
written; every case in the table below carries none. Many older names
across the suites do contain one — the two forms above are how to run those.

## Engine-defect tags (`[engine-defect-*]`)

Tests that pin an **otterbrix engine** defect are tagged
`[engine-defect-<id>]`. Each was hidden — `[.engine-defect-<id>]`, the leading
`.` keeping Catch2 from running or listing the case and
`catch_discover_tests` from registering it, so neither `ctest` nor the
`Dockerfile.test` image build (which runs `ctest`) saw it — for as long as the
defect was open. The engine bump closed all of them, the dots came off, and
every case below now runs in the default run and in `ctest` and is green. A red
case here is an engine regression: fix the engine, do not re-hide the tag.

Three rows below are not engine defects at all: `[ch-describe-routing]`,
`[mysql-describe-routing]` and `[pg-describe-routing]` are acceptance tags over
otterstax's own prepare-time routing, kept in this table so every named group is
in one place. A red case there is an otterstax regression, not an engine one.

| Tag | File | Pins |
|-----|------|------|
| `[engine-defect-d1]` | `tests/system/test_mixed_plan_engine.cpp` | A backend slice (`node_raw_data`, as a ConnectorManager substitutes it) JOINed against a local composite table on a struct-field key (`c.addr_city = (w.location).city`) with a local-side predicate that leaves no rows (`WHERE (w.location).country IN (...)` over an empty table) — `operator_join_t::build_layout_` receives zero build chunks and SIGSEGVs. Four separate TEST_CASEs (struct/plain key × empty/seeded table), never SECTIONs, because the crash kills the process; on rc-2 only the struct-key/empty-table case crashes, the other three are the control and pass. The demo's `step_4` is this shape. |
| `[engine-defect-e1]` | `tests/system/test_composite_insert.cpp` | A composite value with a NULL member — `ROW(..., NULL)` — written through INSERT (full and partial column list, `INSERT ... SELECT`, nested `ROW(ROW(1,NULL),'x')`, a `DEFAULT ROW(...)` column omitted from the INSERT) and `UPDATE ... SET c = ROW(2, NULL)`, plus the demo `step_3b` nine-member `spec_t` INSERT read back through `step_3c`. On rc-2 the INSERTs fail with `insert_node: can not convert data column[n] type to table type`; the same `ROW(...)` without a NULL member is accepted. The UPDATE case fails earlier, in otterstax's own SET-expression translation (`unsupported expression in UPDATE SET`). The DEFAULT case reads back an empty member instead of the declared default — the same defect `[engine-defect-default]` pins on the engine alone. |
| `[engine-defect-default]` | `tests/system/test_default_insert.cpp` | A column DEFAULT whose literal type differs from the declared column type is not stored when the INSERT omits the column: `n INT DEFAULT 7` reads back 0 (the integer literal is BIGINT), `v trio_t DEFAULT ROW(5, 'd', NULL)` reads back a member-wise zero/empty struct (the ROW members are literal-typed). No error anywhere: the engine's `agent_disk_t::storage_append_inner` fills the omitted column with `vector_t::set_value(row, default_value())`, and `set_value` refuses a value whose type differs from the vector's without writing it (assert-only), so the freshly built vector keeps its valid, zeroed slot. Each shape is one case straight against the engine and one through the scheduler stack; the engine-alone case failing the same way is what attributes the defect to the engine. The same-typed DEFAULTs (`BIGINT DEFAULT 7`, `STRING DEFAULT 'x'`, `DOUBLE DEFAULT 1.5`) are the control and run visibly under `[default]`. |
| `[engine-defect-nullparam]` | `tests/system/test_dml_result_shape.cpp` | A prepared `SELECT id FROM dmldb.people WHERE id = $1` executed with a NULL parameter must answer no rows and keep its `id` column, as PostgreSQL answers `id = NULL` with an empty result and a RowDescription. On rc-2 the engine's `full_scan` cuts a comparison with a NULL parameter short with `make_drain_chunk` over an empty type list, so the result has no columns and `OtterbrixManager::execute` refuses it as `schema_error` (`SELECT returned a result without columns: the engine produced no typed output column for the statement`). rc-3's `full_scan` builds that drain chunk from the scan's types (`make_drain_chunk(guard_types_)`), which is what this case accepts. The visible `prepared matrix: a NULL parameter` stays the control: a NULL bound into an INSERT and read back through `IS NULL`. |
| `[engine-defect-having]` | `tests/unit/parser/test_query_gen.cpp` | Two aggregates of one function differing only in their argument: `SELECT category, SUM(a) AS sa, SUM(b) AS sb ... HAVING SUM(b) > 1` must push down `HAVING SUM("b") > 1`. A transformer that resolves a HAVING aggregate by function name alone (`resolve_having_operand`) binds the operand to the first SUM of the SELECT list and writes `SUM("a")`; matching on the argument list (and DISTINCT) too is what this case pins. The only one of these tags outside `test_system`. |
| `[ch-describe-routing]` | `tests/system/test_single_backend_prepare_schema.cpp` | **Not an engine defect — an acceptance tag.** `Worker::prepare_schema` must hand a single-backend ClickHouse statement that carries no parameters to `ClickhouseManager::describe` before it reads the plan stub, so that what a frontend is told at prepare is what the backend answers: `describe` wraps the generated SQL in `SELECT * FROM (…) LIMIT 0` and takes the column types off the header block of the answer. Six cases over the ClickHouse mock. Four prepare and stream one column each: `COUNT(*) AS n` as UBIGINT/uint64 (asserting the probe query is the wrapped data query), `score + 1 AS score` as BIGINT/int64, `length(name) AS name` as UBIGINT/uint64, and a probe the backend refuses failing the prepare with the backend's own `io_error`. Two more cover a raw-SQL subquery stub, which `describe` fills in place rather than replacing the node — the text and qualifiers `execute` generates from must survive: a derived table, whose column reached the client as NA before, and a JOIN with a subquery, whose prepare did not merely degrade but failed outright (`merge_schemas` saw the stub's NA and answered "OtterBrix collection is missing in catalog"). Without that send each case is red with the plan's own type instead — a red case here is that routing gone, not a mock detail. |
| `[mysql-describe-routing]` | `tests/system/test_single_backend_prepare_schema.cpp` | **Not an engine defect — an acceptance tag.** The same routing for MySQL: `Worker::prepare_schema` hands a parameterless statement classified MySQL to `MySQLManager::describe`, which probes every stub with `SELECT * FROM (…) AS otx_prepare_probe LIMIT 0` — MySQL refuses a derived table without an alias, which ClickHouse's wrap does without — and takes the column types off the answer's column definitions, read by `tsl::mysql_to_chunk`, the translator `execute` reads its rows with (`mysql_to_struct` reads the same answer off the same table, so the two cannot disagree; a second copy of the map once did, having no BIGINT case). Four cases over a typed MySQL mock: `score + 1 AS score` prepared and streamed as the BIGINT the backend answers while the plan reads INTEGER (asserting the probe is the wrapped data query), a raw-SQL sub-query column prepared as the backend's type where it reached the client as NA, a JOIN with such a sub-query prepared from both described stubs where the prepare failed outright before, and a probe the backend refuses failing the prepare with its own `io_error`. The actor-level `MySQLManager::describe: ` family (8 cases) covers the refusals and the probe text. |
| `[pg-describe-routing]` | `tests/system/test_single_backend_prepare_schema.cpp` | **Not an engine defect — an acceptance tag.** The same routing for PostgreSQL, over a second typed PostgreSQL mock connector: `PostgressManager::describe` probes every stub the same way and reads the answer with `tsl::pg_to_struct` under the connection's ENUM oids — the translator its `execute` types tuples with. Four cases: `length(name) AS name` prepared and streamed as the INTEGER the backend answers while the plan has no type for the call at all, a raw sub-query column prepared as the backend's type, a JOIN with a raw sub-query prepared from both described stubs, and a refused probe failing the prepare with the backend's error. The actor-level `PostgressManager::describe: ` family (7 cases) covers the refusals and the probe text. |

```bash
# Run one group explicitly
./build/Release/tests/system/test_system "[engine-defect-d1]"
./build/Release/tests/system/test_system "[engine-defect-e1]"
./build/Release/tests/system/test_system "[engine-defect-default]"
./build/Release/tests/system/test_system "[engine-defect-nullparam]"
./build/Release/tests/unit/parser/test_parser "[engine-defect-having]"
./build/Release/tests/system/test_system "[ch-describe-routing]"

# A crashing case must be run on its own to attribute the SIGSEGV
./build/Release/tests/system/test_system "D1: backend slice JOIN local composite table on struct key over empty local table"

# They are ctest cases now, listed by name. The tag itself never appears in the
# listing — `otterstax_discover_tests` runs `catch_discover_tests` with
# ADD_TAGS_AS_LABELS=FALSE — so grepping the listing for a tag reads 0 whether
# the dots are on or off and proves nothing. What hiding a case does move is the
# listing itself: the group's names drop out of it. So count the names — one
# count per group, each a fixed number that only changes when a case is added to
# that group on purpose.
(cd build/Release && ctest -N | grep -c 'E1: ')                        # → 7
(cd build/Release && ctest -N | grep -c 'D1: ')                        # → 4
(cd build/Release && ctest -N | grep -c 'DEFAULT: ')                   # → 6
(cd build/Release && ctest -N | grep -c 'ClickHouse prepare_schema: ') # → 8, 6 of them tagged
(cd build/Release && ctest -N | grep -c 'MySQL prepare_schema: ')      # → 4, all tagged
(cd build/Release && ctest -N | grep -c 'PostgreSQL prepare_schema: ') # → 4, all tagged
# The total is a cross-check, not an invariant: it moves whenever a case is
# added anywhere in the suites, so read it, do not assert it.
(cd build/Release && ctest -N | tail -1)                 # → Total Tests: N
```

Do not weaken or delete these tests to make a run green; the fix belongs in the
engine.

## Python Test Structure

Python tests share common helpers via `config.py` (connection params) and `query_data.py` (expected result fixtures). File names encode: `test_{protocol}_client_{backend}_backend[_mutable].py`. The `test_cross_backend_queries*.py` files exercise mixed-backend joins.

External-table tests (s3/file grammar extension) over the MySQL wire — driven via the shared `external_helpers.py` module and a small family of `test_mysql_*.py` files:

- `test_{schema_,}mysql_{file,s3}.py` — the originals; verify schema discovery, row counts, per-campaign aggregates, `COPY` round-trips, and JOINs across the two joinable fixtures `regions.parquet` (200 rows) and `web_events.csv` (5000 rows).
- `test_mysql_file_ndjson.py` — adds `campaigns.ndjson` (50 rows) to confirm ndjson load + COPY round-trip via the file source.
- `test_mysql_join_sql_s3_to_s3.py` — full federated → external round-trip: `CREATE EXTERNAL TABLE` from s3 parquet, stage backend rows into otterbrix-internal, JOIN, persist, `COPY (…) TO 's3://…'` as csv, read back via a fresh external table.
- `test_mysql_join_otb_local_s3.py` — `CREATE TABLE` + `INSERT VALUES` into otterbrix-internal then JOIN against an s3-loaded external; positive control for the "all sides resolved to engine-internal tables" path.
- `test_mysql_join_otb_local_backend.py` — backend on the LEFT, otterbrix-internal `CREATE TABLE` on the RIGHT, JOIN on a STRING key. Mirrors the shape `examples/demo/sql/step_4.sql` runs in the live demo: backend manager fetches the sql slice through the existing single-backend dispatch and inlines it as `raw_data`; the engine resolves the symbolic local side by its stamped `table_oid` and JOINs them in-process. The string JOIN key avoids the int width sensitivity described under "JOIN-key width sensitivity" below.
- `test_mysql_join_otb_local_s3_file.py` — three-origin JOIN: s3 parquet `regions` ⋈ local file csv `web_events` ⋈ otterbrix-internal `weights` (`CREATE TABLE`+`INSERT VALUES`), all on `campaign_id` (int64 on every side). Smaller-scale shadow of the `external_join_all` benchmark — catches regressions in the "everything resolved to engine-internal" multi-source JOIN before the benchmark suite runs.

Fixtures live in `tests/minio/fixtures/` (regenerable by `generate_external_fixtures.py`; needs `pyarrow` + `faker`). The python suite uses the single-bucket MinIO in `compose.test.yml` (mount `/fixtures` for local-file mode; for s3 mode the `miniotest` alias is registered at server startup from the baked-in `tests/scripts/config.yaml`, so `ExternalTableTester.ensure_credentials()` is a no-op). The standalone two-MinIO debug stack under `tests/minio/` (`docker-compose.yml`, `manual_run.sh`, `s3cmd.sh`) is unrelated to the suite — see `tests/minio/CLAUDE.md`.

**JOIN-key width sensitivity** is the common gotcha in the JOIN tests
above. The engine silently returns zero rows when an equi-JOIN compares an
int32 column against an int64 column — no error. Two places to keep in
sync:

- Local `CREATE TABLE` columns joining against parquet-loaded data: declare
  the local side `bigint` to match the int64 the parquet loader emits.
- Backend `INT` columns (which `mysql_to_chunk` and friends pass through as
  int32) joining against a local `bigint`: either stage the backend slice
  into a local `bigint` table first (`test_mysql_join_sql_s3_to_s3.py`) or
  join on a string column instead (`test_mysql_join_otb_local_backend.py`,
  mirroring `examples/demo/sql/step_4.sql`). The engine-side rule is stated
  in the top-level `CLAUDE.md`, "JOIN-key type widths must agree".

**Cleanup discipline.** `ExternalTableTester.cleanup()` runs DROPs for every
`(db, table)` pair the test appended to `_created`. Most tests in this
family call it at the **end** of `run()` rather than from a `try/finally`,
so a failed `assert` skips the cleanup and leaves engine state behind. That
leak can cascade into the next test in the suite (we hit a 400-row
`regions` count in one run because the previous test failed before
cleaning up). Wrap with `try/finally` when you add a new test here.

Run a single Python test file:
```bash
python -m pytest tests/test_flightsql_client_mysql_backend.py -v
```
(Requires live Docker stack — run `docker compose -f compose.test.yml up -d` first. Connections are baked into the otterstax image from `tests/scripts/config.yaml` and registered at startup; no manual registration step.)

### Convention: one class, each case an explicit `test_*` method

**Do NOT cram every scenario into one giant `main()` with inline blocks — it is unreadable.** Structure an integration test file as a single class whose cases are named methods. Reference: `test_pg_client_pg_backend.py` and the `test_kafka_*.py` family.

- One `class <Name>Test` per file; connection params / topic names / fixtures in `__init__` (read `--local` host via `config.py`).
- `setup(self)` creates the shared objects (CREATE TABLE/SOURCE/STREAM, produce seed data); `cleanup(self)` drops them and closes the connection.
- **Each test case is its own `def test_<behaviour>(self)` with a one-line docstring** — one observable behaviour per method, named for what it asserts.
- Shared helpers (`create_topic`, `consume`, `assert_equal`, …) are methods on the class, not free functions duplicated inline.
- `run_all_tests(self)`: `try: setup(); test_a(); test_b(); finally: cleanup()`. **Let exceptions propagate** — do not `except: print("fail")` and swallow, or the green ALL-PASSED banner will lie (a real bug in some older tests).
- `main_test()` parses `--local`, runs `run_all_tests()`, prints the pass/fail banner, returns `0`/`1`; `sys.exit(main_test())` at the bottom.
(Requires live Docker stack — run `docker compose -f compose.test.yml up -d` first. Connections are baked into the otterstax image from `tests/scripts/config.yaml` and registered at startup; no manual registration step.)
